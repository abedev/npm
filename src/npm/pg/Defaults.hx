package npm.pg;

typedef Defaults = {
  user: String,
  database : String,
  password : String,
  host : String,
  port : Int,
  ssl : Bool,
  rows : Int,
  poolSize : Int,
  poolIdleTimeout : Int,
  reapIntervalMillis : Int,
  binary : Bool,
  parseInt8 : Bool,
};
